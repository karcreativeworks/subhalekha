/** Photos per page for public gallery infinite scroll. */
export const PUBLIC_GALLERY_MEDIA_PAGE_SIZE = 80

/** Max `limit` allowed on the public gallery media API. */
export const PUBLIC_GALLERY_MEDIA_MAX_PAGE_SIZE = 100

/** IntersectionObserver rootMargin — prefetch well before the sentinel is visible. */
export const PUBLIC_GALLERY_SCROLL_ROOT_MARGIN = "2800px 0px"

/** Max consecutive fetches per scroll trigger (fast-scroll catch-up). */
export const PUBLIC_GALLERY_MAX_CHAINED_LOADS = 4
